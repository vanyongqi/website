server:
  env: development
  cors:
    allowOrigins: ["http://localhost:5173"]

log:
  level: debug
  json: false

database:
  dsn: "app:123456@tcp(localhost:3306)/appdb?parseTime=true&charset=utf8mb4&loc=Local"
